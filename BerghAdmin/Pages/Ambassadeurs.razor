@page "/Ambassadeurs"
@inject NavigationManager NavManager

@code
{
    protected async override Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
        NavManager.NavigateTo("/personen?rol=Ambassadeur");
    }

}